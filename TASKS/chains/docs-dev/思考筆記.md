
---

## 2026-04-10T01:13 UTC — docs-dev Chain

### T1 完成 ✅

**任務**：根據已完成的 API 端點，更新 README.md 的 API 文件

**變更內容**：
在 `README.md` 中新增「API 端點參考」章節（置於「支援模型」與「容器管理」之間），完整記錄所有 13 個 API 端點：

| 路由 | 方法 | 認證 | 說明 |
|---|---|---|---|
| `/api/register` | POST | 公開 | 申請新實例，觸發飛書掃碼 |
| `/api/register/poll/:id` | GET | 公開 | 輪詢掃碼狀態 |
| `/api/user/me` | GET | 用戶 JWT | 取得目前用戶實例資訊 |
| `/api/instances` | GET | Admin JWT | 列出所有實例 |
| `/api/instance/:id/set-budget` | POST | Admin JWT | 設定花費上限 |
| `/api/instance/:id/activate` | POST | Admin JWT | 佈建並啟動容器 |
| `/api/instance/:id/start` | POST | Admin JWT | 啟動 Gateway |
| `/api/instance/:id/stop` | POST | Admin JWT | 停止 Gateway |
| `/api/instance/:id/delete` | POST | Admin JWT | 刪除實例 |
| `/api/health` | GET | Admin JWT | 系統總健康狀態（LiteLLM + 模型清單）|
| `/api/health/agent/:agentId` | GET | Admin JWT | 指定容器存活檢查 |
| `/api/health/litellm` | GET | Admin JWT | LiteLLM Proxy 健康檢查 |
| `/api/spend` | GET | Admin JWT | 用戶花費查詢 |

每個端點皆附有請求格式、響應範例與行為說明。

**檔案變更**：
- `README.md` — 新增「API 端點參考」章節
- `BACKLOG.md` — T1 `[ ]` → `[✅]`
- `LOOP.md` — 推進至 T2

**下一任務**：T2 — 為 `provisioner.js` 編寫 JSDoc 注釋

---

## 2026-04-10T02:19 UTC — docs-dev Chain

### T2 完成 ✅

**任務**：為 `provisioner.js` 編寫 JSDoc 注釋

**變更內容**：
為 `provisioner.js` 中所有 8 個導出函數新增 / 強化 JSDoc 注釋：

| 函數 | JSDoc 內容 |
|---|---|
| `provisionAgent` | `@param` 完整參數列表（含可選欄位）、`@returns` 回傳值類型、`@throws` 錯誤情形、`@example` |
| `startGateway` | `@param`、`@returns`、`@throws` |
| `stopGateway` | `@param`、`@returns`、`@example` |
| `deleteInstance` | `@param`、`@returns`、`@example` |
| `isGatewayRunning` | `@param`、`@returns`、`@example` |
| `checkContainerLiveness` | 保留既有內容（已足夠） |
| `checkLiteLLMProxyHealth` | 替換"T14"標記為標準 `@returns`、`@example` |
| `getLiteLLMModelInfo` | 替換"T7"標記為標準 `@param`（master key 環境變數說明）、`@returns`、`@example` |
| `getLiteLLMSpend` | 替換"T8"標記為標準 `@param`、`@returns`、`@example` |
| 模組頂部 | 新增 `@version 5` |

所有注釋均使用 JSDoc 標準格式，含 `@param`、`@returns`、`@throws`、`@example` 等標籤，支援 VS Code / JSDoc 工具鏈推斷類型。

**檔案變更**：
- `provisioner.js` — 新增 / 強化 8 個函數的 JSDoc + 模組 `@version`
- `BACKLOG.md` — T2 `[ ]` → `[✅]`
- `LOOP.md` — 推進至 T3

**下一任務**：T3 — 為 `server.js` 編寫 JSDoc 注釋

---

## 2026-04-10T18:38 UTC — docs-dev Chain（Round 2）

### T3 完成 ✅

**任務**：為 `server.js` 編寫 JSDoc 注釋

**變更內容**：
為 `server.js` 中所有 16 個路由處理函式及 2 個中介層新增標準 JSDoc 注釋：

| 函式 / 路由 | JSDoc 內容 |
|---|---|
| `requireAdmin` | `@param` req/res/next、`@returns`（透過 next 傳遞）|
| `requireUser` | `@param` req/res/next（含 `req.user` 附加說明）、`@returns` |
| `GET /admin` | `@name`、`@route` |
| `GET /api/user/me` | `@name`、`@route`、`@middleware`、`@returns`、`@throws` |
| `POST /api/register` | `@name`、`@route`、`@param`（userNickname/botNickname）、`@returns`、`@throws`、`@example` |
| `GET /api/register/poll/:id` | `@name`、`@route`、`@param`、`@returns`、`@throws`、`@example` |
| `GET /api/instances` | `@name`、`@route`、`@middleware`、`@returns`、`@throws` |
| `POST /api/instance/:id/set-budget` | `@name`、`@route`、`@middleware`、`@param`、`@returns`、`@throws`、`@example` |
| `POST /api/instance/:id/activate` | `@name`、`@route`、`@middleware`、`@param`、`@returns`、`@throws`、`@example` |
| `POST /api/instance/:id/start` | `@name`、`@route`、`@middleware`、`@param`、`@returns`、`@throws`、`@example` |
| `POST /api/instance/:id/stop` | `@name`、`@route`、`@middleware`、`@param`、`@returns`、`@throws`、`@example` |
| `POST /api/instance/:id/delete` | `@name`、`@route`、`@middleware`、`@param`、`@returns`、`@throws`、`@example` |
| `GET /api/health/agent/:agentId` | `@name`、`@route`、`@middleware`、`@param`、`@returns`、`@throws`、`@example` |
| `GET /api/health/litellm` | `@name`、`@route`、`@middleware`、`@returns`、`@throws`、`@example` |
| `GET /api/health` | `@name`、`@route`、`@middleware`、`@returns`、`@throws`、`@example` |
| `GET /api/spend` | `@name`、`@route`、`@middleware`、`@param`（user_id query）、`@returns`、`@throws`、`@example` |
| `app.listen` | `@listens`、`@fires` |

所有路由皆使用 `@name`（JSDoc 命名）、`@route`（route 命名）、`@middleware`、`@param`/`@returns`/`@throws`、`@example` 等標準標籤，支援 VS Code  IntelliSense 與 JSDoc 工具鏈。

**檔案變更**：
- `server.js` — 新增 16 個路由處理函式及 2 個中介層的 JSDoc
- `BACKLOG.md` — T3 `[ ]` → `[✅]`
- `LOOP.md` — 推進至 T4

**下一任務**：T4 — 為所有 API 端點編寫 OpenAPI/Swagger 規格



---

## 2026-04-10T18:56 UTC — docs-dev Chain T4 完成 ✅

### 任務：為所有 API 端點編寫 OpenAPI/Swagger 規格

**產出**：`openapi.yaml`（OpenAPI 3.0.0 規格，27,696 bytes）

**覆蓋範圍**：全部 13 個 API 端點

| 分類 | 端點 |
|---|---|
| 公開 | `POST /api/register`、`GET /api/register/poll/{id}` |
| 用戶 | `GET /api/user/me` |
| 管理員 | `GET /api/instances`、`POST /api/instance/{id}/set-budget`、`POST /api/instance/{id}/activate`、`POST /api/instance/{id}/start`、`POST /api/instance/{id}/stop`、`POST /api/instance/{id}/delete`、`GET /api/health/agent/{agentId}`、`GET /api/health/litellm`、`GET /api/health`、`GET /api/spend` |

**Schema 組件**（14 個）：
- 列舉型別：`InstanceStatus`
- 請求：`RegisterRequest`、`SetBudgetRequest`
- 響應：`UserMeResponse`、`RegisterResponse`、`PollResponse`、`InstanceSummary`、`ContainerOperationResponse`、`ActivateResponse`、`HealthResponse`、`AgentLivenessResponse`、`LiteLLMHealthResponse`、`SpendResponse`
- 通用：`Error`

**技術細節**：
- YAML literal block scalar (`|`) 在多行 description 中，若 continuation 行縮排少於首行，會觸發 YAML 解析錯誤 → 改用單行 description 解決
- 所有錯誤響應使用 `$ref` 統一引用，避免重複定義
- JWT Bearer 認證透過全域 `security` 宣告，自動套用至所有端點（可被局部覆寫）

**驗證**：`python3 yaml.safe_load()` ✅ 語法正確；`13/13` 路徑完整

**推進**：T5 — 實現 Swagger UI 線上展示

---

## 2026-04-10T19:12 UTC — docs-dev Chain

### T5 完成 ✅ — Swagger UI 整合

**任務**：實現 API 文檔的線上展示（Swagger UI）

**變更內容**：
- `package.json` 新增 `swagger-ui-express@^5` 依賴
- `server.js` 新增 2 個路由：
  - `GET /docs` → Swagger UI 互動式頁面
  - `GET /openapi.yaml` → 原始 YAML 規格檔
- 啟動訊息新增 `API Docs: http://localhost:3210/docs`
- Swagger UI 配置：persistAuthorization、displayRequestDuration、docExpansion: 'list'、自訂 CSS
- `node --check server.js` ✅ 語法正確

**Phase 2 完成**：API 文檔（openapi.yaml + Swagger UI）全部就緒

**下一任務**：T6 — 編寫「快速開始指南」（5 分鐘內啟動服務）

---

## 2026-04-10T19:29 UTC — docs-dev Chain（T6 完成）

### 任務：T6 — 編寫「快速開始指南」（5 分鐘內啟動服務）

### 執行摘要
為 Auto-Create OpenClaw 專案新建 `QUICKSTART.md`，目標讀者是希望快速啟動服務的新用戶。

### 變更檔案
| 檔案 | 動作 |
|------|------|
| `QUICKSTART.md` | 新建（2,836 bytes）|
| `BACKLOG.md` | T6 `[ ]` → `[✅]` |
| `LOOP.md` | 推進至 T7 |

### 產出說明
`QUICKSTART.md` 包含：
- 前置需求（Docker、Node.js、WSL2 建議）
- 四步驟流程（clone → .env → start.sh → 驗證）
- JWT_SECRET 產生指令
- 5 個常見 Q&A
- Swagger UI 入口提示（`http://localhost:3210/docs`）

### 下一任務
**T7 — 編寫「管理員操作手冊」**

### 迭代評估
- T6 完成難度：低（文件寫作）
- 與 README.md 的分工：README 面向完整架構理解，QUICKSTART 面向快速上手

---

## 2026-04-10T19:48 UTC — docs-dev Chain Round

### T7 完成 ✅ — 管理員操作手冊

**產出**：`docs/ADMIN_MANUAL.md`（7,647 bytes）

**設計決策記錄**：
- 預算修改（set-budget）不影響已存在 LiteLLM 金鑰的事實被明確記錄，避免管理員誤解"設定後立即生效"
- 明確區分「刪除資料庫記錄」vs「刪除容器」vs「刪除工作區目錄」三個不同層次
- 容器直接運維章節提供 `docker exec` 進入、查看日誌、手動重啟 Gateway 的實際指令
- Standard Operations 章節用「情境 A/B/C/D」編排，直接對應管理員在 Slack/飛書收到的用戶回報
- Base Image rebuild 流程強調「已存在容器不會自動更新」，避免管理員預期錯覺

**與 QUICKSTART.md 的讀者區分**：QUICKSTART 服務新用戶（如何啟動），ADMIN_MANUAL 服務管理員（如何維運）。兩個文件定位清晰，互不重疊。

**下一步**：T8 — 故障排除指南（Troubleshooting Guide），將整合前幾輪已記錄的常見錯誤模式。
